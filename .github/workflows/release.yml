name: Release to crates.io

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write
  packages: write
  id-token: write  # Required for OIDC trusted publishing

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  RUST_BACKTRACE: short
  RUSTFLAGS: "-D warnings"
  RUSTUP_MAX_RETRIES: 10

jobs:
  # Verify the release before publishing
  verify:
    name: Verify Release
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Full history for changelog generation

      - name: Extract version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Releasing version: $VERSION"

      - name: Verify version matches Cargo.toml
        run: |
          CARGO_VERSION=$(grep '^version' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')
          TAG_VERSION="${GITHUB_REF#refs/tags/v}"
          if [ "$CARGO_VERSION" != "$TAG_VERSION" ]; then
            echo "Version mismatch: tag=$TAG_VERSION, Cargo.toml=$CARGO_VERSION"
            exit 1
          fi
          echo "Version verified: $CARGO_VERSION"

      - name: Install Rust nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: llvm-tools-preview

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "release"
          save-if: false

      - name: Check formatting
        run: cargo +nightly fmt --all -- --check

      - name: Run clippy
        run: cargo clippy --workspace --all-features --all-targets -- -D warnings

      - name: Run tests (nextest)
        uses: taiki-e/install-action@v2
        with:
          tool: nextest

      - name: Run tests with nextest
        run: cargo nextest run --workspace --all-features --all-targets --no-fail-fast

      - name: Run doctests
        run: cargo test --workspace --doc --all-features

  # Generate changelog using git-cliff
  generate-changelog:
    name: Generate Changelog
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install git-cliff
        uses: taiki-e/install-action@v2
        with:
          tool: git-cliff

      - name: Generate changelog for this release
        run: |
          git-cliff --tag "${{ github.ref_name }}" --strip all > RELEASE_NOTES.md

      - name: Display release notes
        run: cat RELEASE_NOTES.md

      - name: Upload release notes as artifact
        uses: actions/upload-artifact@v6
        with:
          name: release-notes
          path: RELEASE_NOTES.md

  # Publish to crates.io using trusted publishing (OIDC)
  publish:
    name: Publish to crates.io
    needs: [verify, generate-changelog]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      id-token: write  # Required for OIDC token generation
      contents: read
    environment:
      name: crates-io
      url: https://crates.io/crates/pjson-rs
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: "publish"
          save-if: false

      - name: Install cargo-release
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-release

      - name: Authenticate with crates.io via OIDC
        uses: rust-lang/crates-io-auth-action@v1
        id: auth

      - name: Publish pjson-rs-domain to crates.io
        working-directory: crates/pjs-domain
        run: cargo publish --no-verify

      - name: Wait for pjson-rs-domain to be indexed
        run: sleep 30

      - name: Publish pjson-rs to crates.io
        working-directory: crates/pjs-core
        run: cargo publish --no-verify

      - name: Verify publications
        run: |
          echo "Verifying publications on crates.io..."
          sleep 10
          cargo search pjson-rs | grep -q "pjson-rs" && echo "✓ pjson-rs published successfully"
          cargo search pjson-rs-domain | grep -q "pjson-rs-domain" && echo "✓ pjson-rs-domain published successfully"

  # Create GitHub release
  github-release:
    name: Create GitHub Release
    needs: [verify, generate-changelog, publish]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download release notes
        uses: actions/download-artifact@v7
        with:
          name: release-notes

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          body_path: RELEASE_NOTES.md
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Post-release notifications
  notify:
    name: Release Complete
    needs: [verify, github-release]
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Release notification
        run: |
          echo "Release ${{ needs.verify.outputs.version }} has been successfully published!"
          echo "Repository: https://github.com/${{ github.repository }}"
          echo "Release: https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}"
          echo "crates.io: https://crates.io/crates/pjson-rs"
